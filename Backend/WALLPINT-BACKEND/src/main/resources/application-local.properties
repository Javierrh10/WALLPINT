# === Base de datos local ===
spring.datasource.url=jdbc:postgresql://localhost:5432/wallpint
spring.datasource.username=postgres
spring.datasource.password=3993
spring.datasource.driver-class-name=org.postgresql.Driver

# === JPA / Hibernate ===
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true

# === Jackson: fechas como ISO-8601 (no como array) ===
spring.jackson.serialization.write-dates-as-timestamps=false

# === JWT (clave de desarrollo, NO la de producci�n) ===
jwt.secret=ClaveLocalSoloParaDesarrolloWallpint2026
jwt.expiration-ms=86400000

# === Puerto ===
server.port=8080